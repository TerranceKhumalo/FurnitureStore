import { FurniterShopValidators } from './furniterShopValidators';

describe('WhiteSpaceValidator', () => {
  it('should create an instance', () => {
    expect(new FurniterShopValidators()).toBeTruthy();
  });
});
